# EVA Agent Catalog

> Inventory of every agent/module in Eva as of **2026-07-15**, captured **before** the Agent Builder meta-agent was added.
> Generated by studying `modules/`, `modules/launcher/eva_launcher.py`, `docs/EVA_AGENTS.md`, `SYSTEM_MAP.md`, and the launchd plists.
> Items that could not be fully confirmed from source are marked **(unverified)** — nothing here is invented.

## How to read this

- **Persona names** (Alex, Scout, Nora, Sam, Cole, Mira, Cleo, Dr. Eva, Sage) come from `docs/EVA_AGENTS.md` — a *naming proposal*, not all built. The **modules** below are the concrete code. Where a persona maps to a module it is noted.
- **Trigger** = how the agent starts: FastAPI route, launchd plist, cron, CLI, or manual.
- **Status**: `active` (running code), `stub` (scaffold / stubbed transport), `deprecated`, or `blocked`.

---

## Orchestration / triage brain

### diracatron (Diracatron — Eva's top-level orchestrator & dispatcher)
- **Role:** Eva's **single** orchestration brain (the Elon Musk-style advisor + PM/orchestrator function are **folded in** — no competing triage brains). Eva owns orchestration: all ingestion + actions are triggered through Diracatron, and it decides which agents to invoke. `POST /triage/run` ingests every **open door** (deal-scout scored+gated deals from its SQLite DealStore, eva-state ledger, activity, market signals / revenue paths) and ruthlessly stack-ranks them with a first-principles rationale. `POST /triage/dispatch` is the **dispatch brain**: it takes a goal/intent, uses an LLM (Elon first-principles prompt via `services/remote/claude`, the shared client) to decide which lobes to invoke, triggers them via the data-driven **agent registry**, collects results, and logs the decision + every outcome to eva-state (it also still dispatches a single queued `item_id`). Nightly scheduled digest posts the prioritized stack-rank.
- **Entrypoint:** `modules/triage-brain/main.py` (diracatron.py brain + first-principles rationale, agent_registry.json + registry.py, dispatch_brain.py LLM/heuristic planner, deal_source.py deal-scout+market sources, service.py, store.py sqlite queue + dispatch history, state_client.py, cli.py, launchd/ service + nightly digest plists)
- **Port:** 8784
- **Relations:** **Registry (data-driven, adding a lobe is a config edit)** covers context-api `:8765`, deal-scout `:8766`, content-engine `:8767`, launcher `:8768`, eva-state `:8769`, channels `:8770`, knowledge `:8771`, voice `:8774`, ghl-agent `:8782`, treasurer `:8786`, social-scheduler `:8787`, deployer `:8789`, local-exec `:8790`, ip-scout `:8791`, brand-builder `:8792`. **Reads** deal-scout's DealStore + eva-state + context + signals; **dispatches** to any registered lobe via HTTP (CLI fallback); **writes** every decision/outcome back to eva-state via `state_client` (self-learning moat); best-effort Slack alerts reuse `modules/social-publish/slack_client.py`. Also registered on the launcher `:8768` via lazy import (`/triage/queue|run|dispatch|digest|registry`).
- **Trigger:** on-demand HTTP `GET /triage/queue|registry|history`, `POST /triage/run|dispatch|digest`; CLI (`run|queue|dispatch --goal|dispatch <id>|digest|registry|history`); nightly `StartCalendarInterval` digest via `launchd/com.eva.diracatron-digest.plist`.
- **Status:** active (offline-safe stubs by default; LLM degrades to deterministic heuristic without an API key)

---

## Finance / spend

### treasurer (Treasurer — finance / spend tracker)
- **Role:** Tracks all Eva operational spend (API/LLM credits, subscriptions, marketplace fees, ad spend, deal costs, hosting/domains) against per-category budget caps so Vineet always knows the burn rate. Agents/CLI/HTTP *log* spend to it; it aggregates by category + period (day/week/month), classifies usage (ok / warn ≥80% / over ≥100%), alerts on newly-crossed thresholds, and projects the monthly run-rate. Tight, lean, stdlib-only — no bank integrations yet.
- **Entrypoint:** `modules/finance-tracker/main.py` (finance_tracker.py core, service.py, store.py sqlite spend_events + budgets, state_client.py, cli.py)
- **Port:** 8786
- **Relations:** **Writes** every spend / budget-breach / daily summary back to eva-state `:8769` via `state_client` (`source_surface = treasurer`); **reuses** `modules/social-publish/slack_client.py` for budget-threshold Slack alerts (imported, not duplicated); **logged to by** any spend-incurring agent (diracatron, deal/content/ad surfaces). Also registered on the launcher `:8768` via lazy import (`/finance/track|summary|budget|export|burn`).
- **Trigger:** launcher SERVICES `treasurer`; HTTP `POST /finance/track`, `GET /finance/summary`, `GET|POST /finance/budget`, `GET /finance/export`, `GET /finance/burn`; CLI.
- **Status:** active (scaffold; offline-safe stubs by default; no bank integrations)

---

## Publishing / scheduling

### social-scheduler (Social-Scheduler — native daily LinkedIn + X publisher)
- **Role:** The autonomous daily publisher for the eva-acquisition pipeline. Runs 5 posts/day on a fixed **America/New_York** schedule (08:00, 11:00, 14:00, 15:00, 17:00 ET — correct regardless of the Mac's own TZ). Each post is gated through the existing social-publish Slack approve-per-post flow; on approval it publishes to LinkedIn + X, then **LIKEs** the post and comments/replies the CTA (`DM or comment "Eva-acquisition" to try it for free`), and logs the post + platform IDs. Keeps a 30-day rolling content queue (day-1 pre-seeded, future days drafted by content-engine), never repeating a headline (dedupe by headline hash). Engagement analytics (impressions/likes/comments/clicks over time) sync hourly 8am–6pm ET into **Eva's own local sqlite** — no Postiz, no SaaS DB.
- **Entrypoint:** `modules/social-scheduler/main.py` (scheduler.py ET schedule + day-1 seed, loop.py resilient self-fire background thread, queue.py rolling queue, cta.py LIKE+CTA, analytics.py X metrics + unified store, store.py sqlite content_queue+post_history+analytics, service.py, state_client.py, cli.py)
- **Port:** 8787
- **Relations:** **Reuses** `modules/social-publish` gate + `slack_client` (approve-per-post, imported not duplicated) + `credentials.build_cfg` (config-file-primary/env-fallback cred resolution from `~/.eva/channels_config.json`), `modules/channels` `linkedin_connector` + `twitter_connector` (added LinkedIn `like_post`/`comment_on_post` and X `reply_tweet`/`like_tweet`/`get_tweet_metrics`), and `modules/linkedin-analytics` (LI per-post metrics); **fed by** `modules/content-engine` (future-day drafts); **writes** every published post / analytics sync / loop fire back to eva-state `:8769` via `state_client` (`source_surface = social-scheduler`). Also registered on the launcher `:8768` via lazy import (`GET /schedule`, `POST /schedule/seed|run|sync`, `GET /analytics`).
- **Trigger:** launcher SERVICES `social_scheduler` — a **self-fire daemon loop** starts with the service, computes the next ET slot, sleeps, then fires `run()` (resilient: catches+logs every error, keeps looping, never crashes; idempotent so double-fire is safe; no-ops when `EVA_SOCIAL_SCHEDULER_OFFLINE=1`, disable via `EVA_SOCIAL_SCHEDULER_NO_LOOP=1`). Also HTTP `GET /schedule`, `POST /schedule/seed|run|sync`, `GET /analytics`; CLI. No external cron/launchd timer required.
- **Security:** live LinkedIn/X secrets live **only** in the gitignored local `~/.eva/channels_config.json`; the in-repo template + `channels_api.py` default config ship empty placeholders (the previously-committed LinkedIn token was removed).
- **Status:** active (offline-safe stubs by default; self-fire loop; all data in local sqlite)

---

## Infra / CI-CD

### deployer (Deployer — CI/CD self-update agent)
- **Role:** Eva keeps **everything** current. An event-driven (polling) CI/CD agent that iterates a **configurable list of deploy targets** and, when a remote is ahead, safely deploys it. Two default targets: **`eva`** (`pull_and_restart` — fast-forwards the Eva backend checkout and gracefully restarts only the services whose module code changed) and **`eva-landing`** (`vercel_prod` — fast-forwards the front-end checkout and runs `vercel --prod`). **Safety is paramount** — it auto-restarts a *running* Eva and ships a live site, so every guardrail exists to never break either: fast-forward-**only** pull (any conflict/non-ff → abort + `deploy_skipped_conflict`/`deploy_landing_failed`, deploy nothing, keep looping); restart **only** the services a `git diff` says changed; **gated on no in-flight work** (waits, bounded, if social-scheduler/gate/dispatch is mid-action before restarting that service); vercel failure → abort+log+skip (never ship a broken build); resilient (catches+logs every error, one target crashing never stops the others, never crashes); offline-safe.
- **Entrypoint:** `modules/deployer/main.py` (deployer.py per-target seams: poll + safe ff-only pull + changed-file→service diff + bounded in-flight-gated restart + `vercel --prod`, loop.py resilient 5-hour self-poll daemon, service.py per-target check/status/history + bounded history, state_client.py, cli.py)
- **Port:** 8789
- **Relations:** **Reuses** the `modules/launcher` `SERVICES` table + stop→start restart mechanism (`:8768`, imported/parsed, not duplicated) to map changed `modules/<dir>` paths to services and restart them; **writes** every `deploy_applied` / `deploy_skipped_conflict` / `deploy_failed` / `deploy_landing_applied` / `deploy_landing_failed` back to eva-state `:8769` via `state_client` (`source_surface = deployer`, `track = infra`). Deploy targets, the Vercel token, and other secrets are resolved **config-file-primary** from `~/.eva/channels_config.json` (env fallback; nothing hardcoded). Also registered on the launcher `:8768` via lazy import (`GET /deployer/status`, `POST /deployer/check`, `GET /deployer/history`).
- **Trigger:** launcher SERVICES `deployer` — a **self-poll daemon loop** starts with the service, sleeps the poll interval (5h default, `EVA_DEPLOYER_POLL_INTERVAL_SECONDS` to override), then fires `check()` which iterates **all** targets (poll → dispatch by action → gated restart / vercel deploy; resilient, never crashes; no-ops when `EVA_DEPLOYER_OFFLINE=1`, disable via `EVA_DEPLOYER_NO_LOOP=1`). Also HTTP `GET /deployer/status`, `POST /deployer/check` (manual trigger), `GET /deployer/history`; CLI. No external cron/launchd timer required.
- **Scope:** Multi-target CI/CD. **`eva`** = git ff-only pull + restart changed Eva services; **`eva-landing`** = git ff-only pull + `vercel --prod` (native Vercel production deploy). Targets are configurable (config-file `deploy_targets` / `EVA_DEPLOY_TARGETS` env / built-in default).
- **Security:** no secret is **ever** hardcoded. GitHub remote-SHA lookup uses the `gh` CLI (auth on the Mac) or authenticated `git ls-remote`, honouring `GITHUB_TOKEN`; the **Vercel token** is resolved config-file-primary (`{"vercel":{"token":…}}` in `~/.eva/channels_config.json`) with a `VERCEL_TOKEN` env fallback.
- **Status:** active (offline-safe by default; self-poll loop; fast-forward-only, gated restarts)

---

## Infra / Local-Exec

### local-exec (Local-Exec — the "Mac hands" layer)
- **Role:** A **localhost-only** request/response exec primitive that lets Eva run shell commands on the Mac **on demand, safely** (not a polling loop). Eva asks to run a command; Local-Exec either **auto-runs** it (if it is on a small allowlist of demonstrably-safe ops) or **gates** it behind a **one-tap Slack approval**. Every run is secret-masked and fully audited. This is the generalized **hands** layer; the [deployer](#deployer-deployer--cicd-self-update-agent)'s scoped CI/CD is a **future consumer** of this exec primitive, not a dependency of it. **Safety is paramount** — this is Eva running shell on your Mac.
- **Entrypoint:** `modules/local-exec/main.py` (exec.py runner+allowlist+secret-masking core — `subprocess.run` with `shell=False` + argv list, never a shell string; service.py allowlisted-auto-run / approval-gate orchestration; store.py sqlite audit; approve.py one-tap Slack notifier; state_client.py; cli.py)
- **Port:** 8790 (**binds `127.0.0.1` only** — a startup assertion `assert_localhost_bind` refuses `0.0.0.0`/any non-loopback and fails closed; **never** exposed via cloudflared/any tunnel)
- **Relations:** **Reuses** `modules/social-publish` (`slack_client` + `credentials.build_cfg`, founder approve channel) for the one-tap approval; **writes** every `local_exec_run` / `local_exec_blocked` / `local_exec_approved` / `local_exec_denied` / `local_exec_failed` / `local_exec_expired` back to eva-state `:8769` via `state_client` (`source_surface = local-exec`, `track = infra`). Allowlist is **config-file-primary** in `~/.eva/local_exec_allowlist.json` (in-code default fallback; config can only *narrow* what auto-runs — security validators live in code, never in the editable config). Also registered on the launcher `:8768` via lazy import (`POST /local-exec/exec`, `GET /local-exec/status`, `GET /local-exec/history`, `POST /local-exec/approve`).
- **Trigger:** launcher SERVICES `local_exec` — request/response HTTP `POST /local-exec/exec` `{command, args?, cwd?, triggered_by?, timeout?}`; `GET /local-exec/status`, `GET /local-exec/history?limit=N`; `POST /local-exec/approve` `{run_id, approved}` (one-tap gate, default **300s** timeout → auto-expire); CLI.
- **Scope:** Allowlisted auto-run ops: `git pull|status|diff|log`; `curl localhost:*` / `curl 127.0.0.1:*` (loopback only); service restart via the launcher's own `/start|/stop|/restart` route on `:8768`; `vercel --prod` (only when `cwd` is a real git checkout); env-file token swaps (a single `KEY=VALUE` via `sed`/`python` on `~/.eva/*.json` or `~/Eva/.env`). Anything else → does **not** run; becomes a `pending` run gated behind Slack approval.
- **Security:** no shell (argv list + `shell=False`; pipes/redirects/`;`/`&&`/globbing inert); **secret masking** of stdout/stderr **and** the echoed command/args (Bearer, API-key/secret/token/password assignments, AWS keys, Slack `xox*` / GitHub `ghp_`|`github_pat_` / OpenAI `sk-` tokens, long high-entropy tokens) before anything is returned or persisted (`masked: true` if redacted; **no raw secret is ever persisted**); resilient (every error caught/logged, never crashes); offline-safe (`EVA_LOCAL_EXEC_OFFLINE=1` → `/exec` is a mocked no-op, **never** spawns a subprocess).
- **Status:** active (offline-safe by default; localhost-only bind; allowlist auto-run + Slack-gated approval; full audit)

---

## Brand / strategy

### brand-builder (Brand-Builder — brand strategy/orchestration layer)
- **Role:** Eva's brand **strategy/orchestration** layer. Sits **ABOVE** content-engine (`:8767`, makes content) and social-scheduler (`:8787`, approves + posts): it writes content **BRIEFS** and **never posts**. Approval stays **L1** — drafts only, the user approves before anything goes out (`approval_required: true` on every pipeline and brief). Owns two core objects per category: a **pipeline** (mission, target_audience, current_goal, offer, cta, positioning, proof_assets, content_pillars, voice_rules, success_metric, blueprint_version) and a **blueprint** (audience, market_patterns[date+source_url+confidence], channels, content_archetypes, authority_signals, awareness_loops, cadence, cta_ladder, do_not_say, kpis). Three persistent **personas** (awareness/authority/brand — persistent configs, NOT always-running agents). `POST /brand/plan` turns a pipeline + blueprint + personas into a weekly set of briefs (cadence straight from the blueprint: daily X, 3x/week LinkedIn, weekly newsletter); `POST /brand/queue` emits each brief to content-engine via a `brand_brief_created` eva-state event. The first pipeline (**Eva Growth Agency**) is seeded from `seed/brand_blueprint_eva_growth_agency.md` (the source of truth), parsed into pipeline.json + blueprint.json + personas.
- **Entrypoint:** `modules/brand-builder/main.py` (blueprint.py deterministic md→pipeline+blueprint parser, personas.py persona configs + archetype→persona selection, planner.py weekly cadence→briefs generator, store.py config-file-primary JSON store in `~/.eva/brand_builder/`, loop.py weekly blueprint-staleness refresh daemon, service.py, state_client.py, seed.py CLI)
- **Port:** 8792
- **Relations:** **Reuses** the `modules/social-publish` config-file-primary pattern (`credentials.build_cfg` shape) for `~/.eva/brand_builder/` JSON storage, the `modules/social-scheduler` `state_client` emit pattern, and the launcher SERVICES + lazy-route registration; **feeds** `modules/content-engine` (`:8767`) content briefs via `brand_brief_created` events; **writes** every `brand_pipeline_seeded` / `brand_plan_created` / `brand_brief_created` / `brand_blueprint_stale` back to eva-state `:8769` via `state_client` (`source_surface = brand-builder`, `track = marketing`). Also registered on the launcher `:8768` via lazy import (`GET /brand/status`, `GET /brand/pipelines`, `GET /brand/pipelines/{id}`, `GET /brand/blueprints/{category}`, `POST /brand/seed|plan|queue|refresh`, `GET /brand/briefs`).
- **Trigger:** launcher SERVICES `brand_builder` — a **weekly refresh daemon loop** (`86400*7s`) starts with the service and re-checks blueprints, emitting `brand_blueprint_stale` for any whose `blueprint_version` is older than 7 days (resilient: catches+logs every error, never crashes; no-ops when `EVA_BRAND_OFFLINE=1`, disable via `EVA_BRAND_NO_LOOP=1`). Also HTTP `POST /brand/plan` `{pipeline_id, timeframe}`, `POST /brand/queue` `{brief_ids?|pipeline_id?}`, `POST /brand/seed`, `POST /brand/refresh`; `seed.py` CLI. No external cron/launchd timer required.
- **Security:** all pipeline/blueprint/persona/brief data lives as plain JSON under the gitignored `~/.eva/brand_builder/` (override `EVA_BRAND_DIR`); no secrets. Writes briefs only — never posts, never touches social credentials. Approval stays L1 (drafts only).
- **Status:** active (offline-safe stubs by default; weekly staleness loop; all data in local JSON; 80 offline tests green)

---

## IP / research

### ip-scout (IP-Scout — prior-art triage)
- **Role:** Eva's **L1-autonomy** invention-triage lobe. Runs a **daily incremental** novelty / prior-art triage over invention-idea seeds and surfaces what's worth a **patent attorney's review**. It **NEVER files, submits (to the USPTO or any authority), or asserts patentability** — novelty scores are heuristic signals to prioritise a human attorney's review. **Hybrid sensors:** (1) user-seeded ideas in `~/.eva/ip_ideas.json` (`id, title, description, category, seeded_at, status`) and (2) **Eva-activity mining** — mines the eva-state ledger for repeatable/novel process patterns proposed as machine-sourced disclosure candidates (`mining.py`, v1 stub; NLP clustering is phase-2 behind the same interface). Each pending idea becomes an **invention disclosure** `{idea_id, title, abstract, claims_draft, sensor_source, created_at, novelty_score, confidence_band, prior_art_hits[], status, attorney_review_needed, recommendation}`. **Novelty scoring** (`novelty.py`) is deterministic: `novelty_score ∈ [0,1]` from token overlap against prior art + claim specificity, with a `confidence_band` (low/med/high) from how much prior-art evidence was examined; low confidence never confidently files or drops (caps at monitor). Emits a **daily markdown report** (`~/.eva/ip_scout/reports/<date>.md`) grouped by `file`/`monitor`/`drop` with "needs attorney review" throughout.
- **Entrypoint:** `modules/ip-scout/main.py` (provider.py pluggable prior-art provider [PatentsView + offline mock; USPTO Open Data phase-2 behind the same `PriorArtProvider` interface], novelty.py deterministic novelty scoring + confidence bands, mining.py eva-activity mining stub, report.py daily markdown report, store.py config-file-primary ideas JSON + sqlite disclosures/runs in `~/.eva/ip_scout/`, credentials.py config-file-primary PatentsView key, loop.py daily triage daemon, service.py, state_client.py emit+read, seed.py CLI)
- **Port:** 8791
- **Relations:** **Reuses** the `modules/social-publish` config-file-primary credential pattern (`credentials.build_cfg` — PatentsView key from `~/.eva/channels_config.json` `ip_scout` section, `PATENTSVIEW_API_KEY` env fallback), the `modules/brand-builder`/`social-scheduler` `state_client` emit pattern, and the launcher SERVICES + lazy-route registration; **reads** eva-state `:8769` events for activity mining and **writes** every `ip_idea_seeded` / `ip_scan_run` / `ip_disclosure_created` / `ip_report_written` back to eva-state via `state_client` (`source_surface = ip-scout`, `track = research`, `entity_type = invention_disclosure`). Also registered on the launcher `:8768` via lazy import (`GET /ip/status`, `GET /ip/ideas`, `GET /ip/idea/{id}`, `POST /ip/seed`, `POST /ip/scan`, `GET /ip/history`, `GET /ip/report/{date}`).
- **Trigger:** launcher SERVICES `ip_scout` — a **daily triage daemon loop** (`86400s`) starts with the service and runs an incremental scan over pending ideas, emitting disclosures + a daily report (resilient: catches+logs every provider error, never crashes; no-ops when `EVA_IP_OFFLINE=1`, disable via `EVA_IP_NO_LOOP=1`). Also HTTP `POST /ip/scan` `{report_date?, mine?}`, `POST /ip/seed` `{title, description?, category?}`; `seed.py` CLI.
- **Security:** all idea/disclosure/report data lives locally under `~/.eva/ip_ideas.json` + `~/.eva/ip_scout/` (gitignored; override `EVA_IP_DIR` / `EVA_IP_IDEAS_FILE`). PatentsView key is optional (offline mock without it) and read config-file-primary — no secrets in code. **L1 autonomy:** triages + reports only; never files, submits, or asserts patentability.
- **Status:** active (offline-safe: mocked PatentsView + stubbed eva-state by default; daily triage loop; ideas in JSON + disclosures/runs in sqlite; 92 offline tests green)

---

## Orchestration / console

### launcher (EVA Launcher — Module 7)
- **Role:** Tiny FastAPI control plane on `:8768`. Starts/stops/health-checks all EVA services and hosts delegated gates (social-publish, Apollo→GHL). The Command Center talks to this.
- **Entrypoint:** `modules/launcher/eva_launcher.py`
- **Port:** 8768
- **Relations:** Manages the `SERVICES` dict (screenpipe 3030, logger, context_api 8765, deal_scout 8766, content_engine 8767, channels 8770, knowledge 8771, voice 8774). Lazily imports `social-publish` (gate/store/credentials) and `channels` (apollo_gate/apollo_store/apollo_connector). Serves the landing tracker cache.
- **Trigger:** launchd `com.eva.launcher.plist` / `com.mangotec.eva-launcher.plist`; manual `python eva_launcher.py`.
- **Status:** active

### command-center
- **Role:** React/Vite front-end console (dashboards: AgentPipeline, DealTracker, ContentQueue, ActivityFeed, etc.). Talks to launcher + service APIs.
- **Entrypoint:** `modules/command-center/src/App.tsx` (built to `dist/`)
- **Port:** static build (served separately)
- **Relations:** Consumes launcher `:8768` + individual service ports.
- **Trigger:** manual/static host.
- **Status:** active

### morning-os
- **Role:** Node/React + Express dashboard ("morning OS"), Lovable-built. Goals/Checkin/Activity/History pages.
- **Entrypoint:** `modules/morning-os/server/index.ts` (routes in `server/routes.ts`)
- **Port:** 5000 (env `PORT` override) **(unverified)**
- **Relations:** Drizzle ORM; standalone server.
- **Trigger:** `npm start` / ts-node.
- **Status:** active

---

## State & knowledge (the memory layer)

### eva-state
- **Role:** Append-only state ledger — single source of truth across agents (events, projects, blockers, coined terms). ~55 seeded events.
- **Entrypoint:** `modules/eva-state/main.py` (service.py, memory.py, project_map.py, directive.md)
- **Port:** 8769
- **Relations:** Written to by many agents via `state_client.py` shims (e.g. ghl-agent, media-editor). Backs `project_map` (Kalpawriksha tree).
- **Trigger:** launchd `com.eva.eva-state.plist`.
- **Status:** active

### knowledge
- **Role:** Living knowledge base — culture, strategy, experiments, deals, playbooks, principles.
- **Entrypoint:** `modules/knowledge/knowledge_api.py` (knowledge_config.py)
- **Port:** 8771
- **Relations:** Google Drive/Docs for doc storage; data under `modules/knowledge/data/*.md`.
- **Trigger:** launchd `com.eva.knowledge.plist` + launcher.
- **Status:** active

### kb_index
- **Role:** Shared "Master Index" writer for Google Docs. Protocol-behind-transport (Stub offline vs GoogleDocsIndexTransport). Library, not a server.
- **Entrypoint:** `modules/kb_index/index_writer.py`
- **Port:** none (imported)
- **Relations:** Used by monetizing-agent (and per docs, a book-agent). Lazy `googleapiclient`.
- **Trigger:** imported by agent modules; deterministic Stub by default.
- **Status:** active (stub transport default)

### intelligence
- **Role:** Deal + signal databases exposed as mountable FastAPI subrouters (no standalone main). `deals_router` (tier/status/asset filters), `signals_router` (+ monthly validation cron), plus `landing_tracker.py`.
- **Entrypoint:** `modules/intelligence/eva_deal_db/deals_router.py`, `modules/intelligence/eva_signal_db/signals_router.py`, `modules/intelligence/landing_tracker.py`
- **Port:** none (mounted in a parent app)
- **Relations:** `DealRepository` / `SignalRepository` (SQLite); `landing_tracker` calls GHL + landing page and caches JSON the launcher serves at `/landing_status`.
- **Trigger:** included in parent FastAPI app; `landing_tracker.py --json` run on demand / scheduled.
- **Status:** active (library pattern)

### logger
- **Role:** Activity logger daemon — window/app usage, idle time, work sessions → JSONL. Plus `eva_context_api.py` (HTTP context API) and screenpipe/activitywatch bridges.
- **Entrypoint:** `modules/logger/eva_logger.py`; `modules/logger/eva_context_api.py`
- **Port:** context_api 8765; logger daemon has no port
- **Relations:** Feeds content-engine + brief generation with activity context.
- **Trigger:** launchd `com.eva.logger.plist` + `com.eva.context-api.plist`.
- **Status:** active

---

## Deal engine

### deal-scout (Scout)
- **Role:** Deal CRUD microservice; pipeline staging/archive, scoring trigger, listing import.
- **Entrypoint:** `modules/deal-scout/main.py` (analyzer.py, database.py, scrapers/flippa.py, scrapers/empire_flippers.py)
- **Port:** 8766
- **Relations:** `analyzer.py` scoring; scrapers pull Flippa / Empire Flippers listings.
- **Trigger:** launchd `com.eva.deal-scout.plist` + launcher.
- **Status:** active

### deal-analyzer-agent
- **Role:** "First agentic-operating-model" service — scores deals via a v7 engine behind HTTP, with cost gate + memory + directive.
- **Entrypoint:** `modules/deal-analyzer-agent/main.py` (agent.py `DealAnalyzerAgent`, scoring_v7.py, radar.py, enrichment.py, learn.py, cost_gate.py, directive.md)
- **Port:** 8767 per header comment **(unverified — collides with content-engine; confirm before running both)**
- **Relations:** `memory.db`, `cost_gate` (spend governance), scoring_v7.
- **Trigger:** manual HTTP + a `run_deal_pipeline()` cron stub.
- **Status:** active (port conflict noted)

---

## Capture → nurture → outreach

### ghl-agent (Nora)
- **Role:** Owned Eva→GoHighLevel service. Idempotent campaign/funnel build + lead-capture automation + webhook handler (the landing page's `/lead/capture` target).
- **Entrypoint:** `modules/ghl-agent/main.py` (service.py, campaign.py `render_touches`, ghl_client.py, oauth.py, state_client.py, memory.py, directive.md)
- **Port:** 8782
- **Relations:** GHL API via **OAuth 2.0 auto-refresh** (`oauth.py` `GHLTokenProvider`: config-file-primary `ghl.oauth` creds → self-refreshing access token, preemptive refresh, 401→refresh→retry, `ghl_oauth_failed` to eva-state; deprecated static `GHL_ACCESS_TOKEN` fallback); writes to eva-state via state_client; Apollo pipeline enrolls contacts here. Renders the 7-touch copy.
- **Trigger:** launchd `com.eva.ghl-agent.plist`; HTTP; exposed via cloudflared tunnel.
- **Status:** active

### channels (Sam-adjacent) + Apollo pipeline
- **Role:** Multi-platform posting hub (LinkedIn, Reddit, Twitter, Facebook) with tone adaptation + signal store; ALSO hosts the Apollo→GHL cold-outreach pipeline (most recent addition).
- **Entrypoint:** `modules/channels/channels_api.py`; Apollo: `apollo_connector.py` (People search, stdlib urllib), `apollo_store.py` (sqlite batches), `apollo_gate.py` (extract→dedup→Slack approve→GHL enroll), `enrolled_contacts.py` (dedup ledger).
- **Port:** 8770
- **Relations:** Apollo gate imports `ghl_client`, posts to Slack DM `D0ARUK4JEDA`, dedups against `enrolled_contacts` (Postgres primary / sqlite fallback), enrolls into GHL location `kyK4yAY6Hur3F4deCx2n` firing workflow `8024cff0`. Connectors post to social platforms.
- **Trigger:** launchd `com.eva.channels.plist` + launcher; Apollo via launcher routes `/apollo/*`.
- **Status:** active

### social-publish (Sam)
- **Role:** Approve-then-publish gate for social. Nothing publishes without explicit Slack ✅ / `approve` or a launcher approval endpoint.
- **Entrypoint:** `modules/social-publish/gate.py` (store.py sqlite, slack_client.py stdlib urllib, credentials.py, cli.py)
- **Port:** none (delegated behind launcher `/social/*`)
- **Relations:** Publishes via `channels` connectors (`linkedin_connector.post_to_linkedin`, `twitter_connector.post_tweet`); Slack for approval. `SLACK_BOT_TOKEN` from env.
- **Trigger:** launcher routes `/social/submit|approve|reject|status|check-approvals`; CLI.
- **Status:** active

### outreach
- **Role:** Approval-gated outreach queue + accredited-investor verification + global suppression list + compliance ledger.
- **Entrypoint:** `modules/outreach/main.py` (service.py, sender.py stub, database.py, models.py)
- **Port:** 8768 per code **(unverified — collides with launcher; confirm)**
- **Relations:** `sender.py` is a stub logger; compliance ledger in sqlite.
- **Trigger:** HTTP (POST /campaigns, /contacts, approval gates).
- **Status:** active (sender stubbed)

### pathfinder
- **Role:** Monetization funnel agent — lead scoring/routing, pipeline waitlist → cold → warm → conversion.
- **Entrypoint:** `modules/pathfinder/pathfinder_api.py` (pathfinder_db.py, outreach_sequences.py)
- **Port:** 8773
- **Relations:** outreach sequences; lead scoring.
- **Trigger:** launchd `com.eva.pathfinder.plist`; POST `/pathfinder/lead`.
- **Status:** active

### email_agent
- **Role:** Morning triage — scans Gmail → deals.db, extracts broker contacts/URLs, builds morning-brief JSON.
- **Entrypoint:** `modules/email_agent/email_agent.py`
- **Port:** none
- **Relations:** Gmail + Google Calendar APIs; `channels_config.json`; sqlite deals.db.
- **Trigger:** launchd daily ~7am **(unverified)** / manual.
- **Status:** active

---

## Content & media

### content-engine (Cole)
- **Role:** Drafts activity summaries → LinkedIn posts; schedules voice-DNA publishing.
- **Entrypoint:** `modules/content-engine/main.py` (generator.py, scheduler.py, voice_dna.py, database.py)
- **Port:** 8767
- **Relations:** Pulls activity context (logger); publishes via linkedin module; voice_dna style.
- **Trigger:** launchd `com.eva.content-engine.plist` + launcher.
- **Status:** active

### postcards
- **Role:** Quote-cards → PNG render (Adam-Grant style) → scheduled publish queue → LinkedIn.
- **Entrypoint:** `modules/postcards/main.py` (renderer.py PIL, publisher.py stub, service.py, database.py)
- **Port:** 8778 **(unverified)**
- **Relations:** renderer (PIL), publisher stub, linkedin_post.
- **Trigger:** HTTP POST `/cards/seed`, PATCH approve, POST `/tick`.
- **Status:** active (publisher stubbed)

### media-editor
- **Role:** Auto video-editor — branded lower-third + loudnorm + music duck + caption cover. Durable jobs.
- **Entrypoint:** `modules/media-editor/main.py` (state_client.py)
- **Port:** 8783
- **Relations:** ffmpeg (bundled fonts in `assets/`); writes eva-state; `state/jobs.json` durable.
- **Trigger:** HTTP POST `/edit`, `/jobs`, `/jobs/{id}`; launchd `com.eva.media-editor.plist`.
- **Status:** active (requires ffmpeg on host)

### linkedin
- **Role:** LinkedIn OAuth handler + CLI post/analytics.
- **Entrypoint:** `modules/linkedin/post.py`, `modules/linkedin/oauth_handler.py`
- **Port:** oauth_handler 8773 **(unverified — collides with pathfinder)**
- **Relations:** `channels_config.json`; LinkedIn v2 API.
- **Trigger:** CLI `python post.py "text"`; manual OAuth flow.
- **Status:** active

### linkedin-analytics
- **Role:** Post analytics sync (impressions/clicks/reactions), idempotent cron-safe snapshots, ledger-recorded.
- **Entrypoint:** `modules/linkedin-analytics/main.py` (service.py, client.py, database.py)
- **Port:** 8780 **(unverified)**
- **Relations:** LinkedIn API client; records to ledger.
- **Trigger:** launchd **(unverified)**; POST `/sync` or `/tick`.
- **Status:** active

### voice
- **Role:** Always-on Mac voice service — wake word ("Hey EVA") + Whisper transcription + ElevenLabs TTS + command routing.
- **Entrypoint:** `modules/voice/voice_service.py`
- **Port:** 8774
- **Relations:** pvporcupine, Whisper, ElevenLabs API, anthropic (Haiku fallback); FastAPI + WebSocket.
- **Trigger:** launchd `com.eva.voice.plist` + launcher.
- **Status:** active (pyaudio needs portaudio per SYSTEM_MAP)

---

## Revenue / monetization

### monetizing-agent (Mira)
- **Role:** Governed successor to Yaksha (angel3). Weekly Mine→Match→Package→Route→Follow-up revenue-leak plays with approval gate + immutable ledger.
- **Entrypoint:** `modules/monetizing-agent/main.py` (brain.py `MonetizationBrain` Protocol, scan.py, mining.py, playbook.py, service.py, memory.py, directive.md)
- **Port:** 8772
- **Relations:** kb_index for Master Index; memory ledger; approval gate.
- **Trigger:** launchd `com.eva.monetizing.plist` (weekly Sunday); manual POST `/scan`.
- **Status:** active

### angels/angel0_sentinel
- **Role:** Watchdog — monitors service health (ports 8765–8771) and auto-restarts dead services with consecutive-failure tracking.
- **Entrypoint:** `modules/angels/angel0_sentinel/sentinel.py`
- **Port:** none (daemon)
- **Relations:** Pings context-api, deal-scout, content-engine, launcher, channels, knowledge (+ lovable-bridge).
- **Trigger:** launchd `com.eva.sentinel.plist` (KeepAlive).
- **Status:** active

### angels/angel3_monetization (Yaksha)
- **Role:** Weekly revenue-leak scan predecessor to monetizing-agent (ungoverned OpenAI calls).
- **Entrypoint:** `modules/angels/angel3_monetization/angel3_monetization.py`
- **Relations:** OpenAI, Google Drive.
- **Trigger:** launchd `com.eva.angel3.plist` (removed from autostart).
- **Status:** **deprecated** (2026-07-09, see `DEPRECATED.md`) — superseded by monetizing-agent.

---

## Integrations / bridges

### lovable-bridge
- **Role:** Wraps Lovable "build-with-URL" API, injects EVA context, and clones Lovable GitHub repos into `~/Eva/modules/`.
- **Entrypoint:** `modules/lovable-bridge/eva_lovable_bridge.py`
- **Port:** 8769 per code **(unverified — collides with eva-state; confirm)**
- **Relations:** Lovable API, git clone, sqlite3.
- **Trigger:** manual HTTP POST `/build` or `/import`.
- **Status:** active (port conflict noted)

### shopify
- **Role:** Shopify OAuth handler / Admin API token exchange.
- **Entrypoint:** `modules/shopify/shopify_auth.py`, `oauth_handler.py`
- **Port:** 8772 **(unverified — collides with monetizing-agent)**
- **Relations:** `channels_config.json`; Shopify Admin API.
- **Trigger:** GET `/shopify/install` → callback `/shopify/callback`.
- **Status:** active

### drive_organizer
- **Role:** Auto-categorizes uploaded Google Drive files (Architecture, Deal Intelligence, Personal Brand, …).
- **Entrypoint:** `modules/drive_organizer/drive_organizer.py`
- **Port:** none
- **Relations:** google-api-python-client, OAuth2 creds.
- **Trigger:** manual / post-export.
- **Status:** active

### projects
- **Role:** Roadmap tree (SQLite) → collapsible mind-map HTML; every edit in an append-only ledger.
- **Entrypoint:** `modules/projects/main.py` (service.py, database.py, models.py)
- **Port:** 8779 **(unverified)**
- **Relations:** sqlite; renders `templates/map.html`.
- **Trigger:** HTTP GET `/`, POST `/api/nodes`.
- **Status:** active

### autostart
- **Role:** Not an agent — boot/install tooling. launchd installers, wake daemon, screenpipe watchdog, status scripts.
- **Entrypoint:** `modules/autostart/eva-install-services.sh` + `launchd/*.plist`
- **Trigger:** manual install.
- **Status:** active (tooling)

---

## RELATIONS — who calls whom + data flow

```
   ┌────────────────────────────────────────────────────────────────────────┐
   │  Diracatron  :8784  — TOP-LEVEL AUTONOMOUS TRIAGE BRAIN                   │
   │  reads eva-state :8769 + activity :8765 + signals → ranks priorities →   │
   │  dispatches to core agents → logs every decision back to eva-state       │
   └─┬─────────────┬─────────────┬─────────────┬───────────────┬──────────────┘
     ▼             ▼             ▼             ▼               ▼
 ghl-agent    pathfinder    deal-scout    monetizing-    social-publish /
 :8782        :8773         :8766         agent :8772    content-engine

                         ┌──────────────────────────┐
   Command Center (UI) ─▶│  launcher  :8768         │
   morning-os (UI)       │  (control plane + gates) │
                         └───────┬──────────────────┘
        start/stop/health        │ lazy-imports gates
   ┌─────────────┬────────────┬──┴────────┬───────────────┐
   ▼             ▼            ▼           ▼                ▼
 logger      deal-scout   content-     channels        voice
 :8765       :8766        engine       :8770           :8774
 (activity)  (Scout)      :8767        (posting +      (wake word)
   │            │         (Cole)       Apollo pipe)
   │            │            │            │
   │            ▼            ▼            ▼
   │        analyzer/     linkedin     apollo_gate ──▶ ghl_client ──▶ GHL
   │        scoring_v7    module       (extract→dedup   location
   │                                    →Slack ✅→enroll) kyK4… → wf 8024cff0
   │                                        │
   │                                        ├─ enrolled_contacts (dedup ledger)
   │                                        └─ Slack DM D0ARUK4JEDA
   │
   ▼
 content context ─▶ content-engine / email_agent morning brief

 social-publish (gate) ──▶ channels connectors (LinkedIn/X)  [via launcher /social/*]
                       └──▶ Slack DM D0ARUK4JEDA for approval

 ghl-agent :8782 ◀── landing page /lead/capture (via cloudflared tunnel)
   └── state_client ─▶ eva-state :8769 (append-only ledger)

 media-editor :8783 ── state_client ─▶ eva-state :8769
 monetizing-agent :8772 ── kb_index ─▶ Google Docs Master Index
 intelligence.landing_tracker ─▶ GHL + landing ─▶ cache ─▶ launcher /landing_status

 angel0_sentinel ── health-pings ─▶ (8765,8766,8767,8768,8770,8771,…) auto-restart

 eva-state :8769  ◀── written by: ghl-agent, media-editor, monetizing-agent,
                                    projects, postcards, linkedin-analytics,
                                    diracatron (triage decisions) (ledgers)
                  ──▶ read by: diracatron (top-level triage brain)
```

### Key data-flow chains
1. **Acquisition funnel:** landing page → `ghl-agent /lead/capture` → GHL contact+opportunity+7-touch workflow → eva-state ledger.
2. **Cold outreach:** `channels/apollo_gate` extract → dedup (`enrolled_contacts`) → Slack approval → `ghl_client` enroll → GHL workflow `8024cff0`.
3. **Social publish:** producer → `social-publish` gate → Slack approval → `channels` LinkedIn/X connectors.
4. **Content:** `logger` activity → `content-engine`/`email_agent` → drafts → linkedin/postcards.
5. **State backbone:** every agent writes outcomes to `eva-state :8769` via `state_client` (self-learning moat).
6. **Health:** `angel0_sentinel` watches ports and restarts; `launcher` is the human/console entrypoint.

### Persona → module map (`docs/EVA_AGENTS.md`)
| Persona | Loop phase | Concrete module | Status in docs |
|---|---|---|---|
| Eva (Orchestrator) | all | launcher + command-center | core |
| Alex (Analyst) | ideation/research | — (proposed, not built) | NEW |
| Scout (Deal Scout) | product engine | deal-scout / deal-analyzer-agent | exists |
| Nora (Nurture) | capture+nurture | ghl-agent | exists |
| Sam (Social) | outbound social | social-publish + channels | in progress |
| Cole (Content) | content engine | content-engine | NEW/partial |
| Mira (Monetizing) | revenue leaks | monetizing-agent | exists |
| Cleo (Citation/AEO) | LLM visibility | — (future) | future |
| Dr. Eva (Scientist) | whitepapers | docs/whitepapers/* | WP01 written |
| Sage (Forever Student) | meta/learning | — (proposed) | NEW |

---

## Port map (observed; conflicts flagged)

| Port | Service | Note |
|---|---|---|
| 3030 | screenpipe | external |
| 5000 | morning-os | unverified |
| 8765 | logger context_api | |
| 8766 | deal-scout | |
| 8767 | content-engine | **deal-analyzer-agent also claims 8767 (unverified conflict)** |
| 8768 | launcher | **outreach also claims 8768 (unverified conflict)** |
| 8769 | eva-state | **lovable-bridge also claims 8769 (unverified conflict)** |
| 8770 | channels | |
| 8771 | knowledge | |
| 8772 | monetizing-agent | **shopify also claims 8772 (unverified conflict)** |
| 8773 | pathfinder | **linkedin oauth_handler also claims 8773 (unverified conflict)** |
| 8774 | voice | |
| 8778 | postcards | unverified |
| 8779 | projects | unverified |
| 8780 | linkedin-analytics | unverified |
| 8782 | ghl-agent | |
| 8783 | media-editor | |
| 8784 | diracatron | top-level autonomous triage brain |
| 8786 | treasurer | finance / spend tracker (8785 reserved for Forge coding-agent) |
| 8787 | social-scheduler | native daily LinkedIn + X publisher (5-slot ET schedule) |
| 8789 | deployer | Multi-target CI/CD agent (5-hour GitHub poll; eva ff-only self-deploy + eva-landing vercel --prod) |
| 8790 | local-exec | "Mac hands" — localhost-only on-demand shell exec (allowlist auto-run + one-tap Slack approval gate; secret-masked + audited) |
| 8791 | ip-scout | prior-art triage — L1 daily novelty/prior-art triage over invention-idea seeds; surfaces attorney-review candidates (never files) |
| 8792 | brand-builder | brand strategy/orchestration layer — writes content briefs (never posts; approval L1); weekly blueprint-staleness loop |

_Port conflicts are marked **(unverified)** — they reflect header comments in code that may not all run simultaneously. Confirm on the host before co-running._

## Auto-inventory

<!-- AGENT-BUILDER:AUTO-INVENTORY:BEGIN -->
### Auto-inventory (generated 2026-07-15T18:08:00.798644+00:00)

| Module | Entrypoint | Port | Trigger | Status |
|---|---|---|---|---|
| agent-builder | `agent_builder.py` | — | cli/manual | active |
| angel0_sentinel | `sentinel.py` | — | launchd | active |
| angel3_monetization | `angel3_monetization.py` | — | launchd | deprecated |
| channels | `main.py` | — | cli/manual | active |
| content-engine | `main.py` | 8767 | route/HTTP | active |
| deal-analyzer-agent | `main.py` | — | cli/manual | active |
| deal-scout | `main.py` | — | cli/manual | active |
| drive_organizer | `drive_organizer.py` | — | cli/manual | active |
| email_agent | `email_agent.py` | — | cli/manual | active |
| eva-state | `main.py` | 8769 | launchd | active |
| ghl-agent | `main.py` | 8782 | launchd | active |
| intelligence | `landing_tracker.py` | — | cli/manual | active |
| kb_index | `__init__.py` | — | cli/manual | active |
| knowledge | `knowledge_api.py` | — | cli/manual | active |
| launcher | `eva_launcher.py` | 8768 | launchd | active |
| linkedin | `oauth_handler.py` | — | cli/manual | active |
| linkedin-analytics | `main.py` | — | cli/manual | active |
| logger | `eva_activitywatch_bridge.py` | — | cli/manual | active |
| lovable-bridge | `eva_lovable_bridge.py` | 8769 | route/HTTP | active |
| media-editor | `main.py` | — | launchd | active |
| monetizing-agent | `main.py` | — | launchd | active |
| outreach | `main.py` | — | cli/manual | active |
| pathfinder | `pathfinder_api.py` | 8773 | route/HTTP | active |
| postcards | `main.py` | — | cli/manual | active |
| projects | `main.py` | — | cli/manual | active |
| shopify | `oauth_handler.py` | — | cli/manual | active |
| social-publish | `cli.py` | — | cli/manual | active |
| triage-brain | `main.py` | 8784 | launchd | active |
| voice | `voice_service.py` | 8774 | route/HTTP | active |
| waitlist | `—` | — | cli/manual | active |
<!-- AGENT-BUILDER:AUTO-INVENTORY:END -->
